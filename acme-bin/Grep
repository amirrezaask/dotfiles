#!/usr/bin/env bash

if [ -x "$(command -v rg)" ]; then
	rg --column --line-number --no-heading --ignore-case "$1"
elif [ -x "$(command -v git)" ]; then
	git grep -n --column "$@"
else
	grep -e "$1" -n -r
fi