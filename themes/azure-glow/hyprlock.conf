$color = rgba(10,10,20,0.9)           # Deep abyss blue
$inner_color = rgba(15,25,45,0.8)     # Midnight navy
$outer_color = rgba(0,170,255,1.0)    # Neon azure
$font_color = rgba(180,220,255,1.0)   # Frosted cyan
$check_color = rgba(0,255,255,1.0)    # Electric aqua
$fail_color = rgba(0,120,255,1.0)     # Alert blue
$input_color = rgba(0,50,80,0.5)      # Deep sea shadow

general {
    grace = 0
    hide_cursor = true
}

background {
    monitor =
    path = ~/.config/omarchy/themes/azure-glow/backgrounds/background.png
    blur_size = 5
    blur_passes = 2
    noise = 0.02
    brightness = 0.85
}

label {
    monitor =
    text = $TIME
    color = $font_color
    font_size = 64
    font_family = JetBrainsMono Nerd Font Bold
    position = 0, 100
    halign = center
    valign = center
}

label {
    monitor =
    text =                     ~~~            ~~~            ~~~
    color = $font_color
    font_size = 24
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, 360
    halign = center
    valign = center
}

label {
    monitor =
    text =                  ~~    ~~      ~~    ~~      ~~    ~~
    color = $font_color
    font_size = 24
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, 355
    halign = center
    valign = center
}

label {
    monitor =
    text =                ~~        ~~  ~~        ~~  ~~        ~~
    color = $font_color
    font_size = 24
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, 350
    halign = center
    valign = center
}

label {
    monitor =
    text =              ~~            ~~            ~~            ~~
    color = $font_color
    font_size = 24
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, 345
    halign = center
    valign = center
}

label {
    monitor =
    text =                ~~        ~~  ~~        ~~  ~~        ~~
    color = $font_color
    font_size = 24
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, 340
    halign = center
    valign = center
}

label {
    monitor =
    text =                  ~~    ~~      ~~    ~~      ~~    ~~
    color = $font_color
    font_size = 24
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, 335
    halign = center
    valign = center
}

label {
    monitor =
    text =                     ~~~            ~~~            ~~~
    color = $font_color
    font_size = 24
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, 330
    halign = center
    valign = center
}
