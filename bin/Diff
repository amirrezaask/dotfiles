#!/usr/bin/env bash
base=`git rev-parse --show-toplevel`
filename=`/bin/cat -`
filename=`echo "$filename" | /bin/sed 's/^[ \t]*//'`
filename="${base}/${filename}"


git diff --no-color "$filename"

#if [[ $? -ne 0 ]]; then
#	git diff --no-color --staged "$filename"
#fi
