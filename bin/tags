#!/usr/bin/env bash

filename=`echo $samfile`

extension=`echo "${filename##*.}"`

base='|c |i+ |i- Cheat Doc Find Grep Comp Def Refs Impls Sig'

if [ $extension == 'go' ]; then
	base="$base Build Run Format Test"
fi

printf "$base" | 9p write acme/$winid/tag